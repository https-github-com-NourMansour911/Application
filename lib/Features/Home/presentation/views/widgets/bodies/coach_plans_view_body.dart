import 'package:flutter/material.dart';

class CoachPlansViewBody extends StatelessWidget {
  const CoachPlansViewBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
